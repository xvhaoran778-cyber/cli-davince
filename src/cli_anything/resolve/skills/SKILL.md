---
name: cli-anything-resolve
description: Control DaVinci Resolve editing workflows from a structured command line.
---

Run `cli-anything-resolve doctor`, then use `--json` commands or `plan validate` and `plan apply`. Resolve must already be running. Use absolute filesystem paths and avoid replacement or deletion unless explicitly requested.
